
use "../../system"
use "../../blocktypes"
use ".."

actor ModbusRtu is Driver
  new create(context':SystemContext, blocktypes':BlockTypes) =>
    None

  be start() =>
    None

  be stop() =>
    None
